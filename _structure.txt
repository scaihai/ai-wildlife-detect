file1/
├── README.md                   # Contains the required run commands to host the app
├── app.py                      # Main Python file for your Flask application
├── Dockerfile                  # Docker build file for your Flask app
├── docker-compose.yaml         # Separate Docker Compose file to orchestrate everything
├── static/                     # Standard Flask folder for CSS, JS, and assets
│   └── style.css               # Fully commented CSS file
├── templates/                  # Standard Flask folder for HTML templates
│   └── index.html              # Fully commented HTML file
├── screenshots/                # Folder for your required terminal screenshots
│   ├── docker_build.png        # Screenshot of Docker build commands
│   └── tf_server_run.png       # Screenshot of hosting TF model server with network bindings
└── models/                     # TensorFlow model serving directory structure
    └── your_model_name/        # The name of your specific model
        └── 1/                  # Model version number (Required by TF Serving)
            ├── saved_model.pb  # Your associated model file
            └── saved_model.pbtxt # (If applicable) text representation of the model